# --- Provider keys (REQUIRED to run a phase; there is no mock fallback) ---
# Writer uses Anthropic, Producer/Art Director use OpenAI, Critic uses Google.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=

# --- Model IDs ---
OPENAI_MODEL=gpt-4.1
ANTHROPIC_MODEL=claude-3-7-sonnet-latest
GOOGLE_MODEL=gemini-2.5-pro
TRANSCRIBE_MODEL=whisper-1

# --- Image generation (ComfyUI must be reachable with a real workflow graph) ---
COMFYUI_URL=http://localhost:8188
COMFY_WORKFLOW=txt2img-reference
COMFY_TIMEOUT_S=180

# --- Infra ---
DATABASE_URL=sqlite:///./movieai.db
QDRANT_URL=http://qdrant:6333
MINIO_ENDPOINT=minio:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=movieai

# --- App ---
API_TOKEN=change-me
TICK_MS=2000
MAX_CONCURRENT_GENERATIONS=2
MAX_GENERATION_RETRIES=3
DAILY_TOKEN_BUDGET=2000000
DAILY_GENERATION_BUDGET=500
APPROVAL_MIN_BRIEF_MATCH=7
APPROVAL_MIN_STYLE=7
FRONTEND_ORIGIN=http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:8000
